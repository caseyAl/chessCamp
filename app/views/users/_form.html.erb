

<% if logged_in? && current_user.role?(:user_id) %>

  <%= simple_form_for(@user) do |f| %>
  <%= f.error_notification %>
  <div class="row">
    <div class="form-inputs">
      <%= f.input :role, label_html: { class: 'active'} %>
      <%= f.input :username, label_html: { class: 'active' } %>
      <%= f.input :password, label_html: {class: 'active'} %>
      <%= f.input :password_confirmation, label_html: {class: 'active'} %>
      <%= f.input :phone, label_html: { class: 'active' } %>
      <%= f.input :email, label_html: { class: 'active' } %>
      <%= f.input :active, as: :hidden, input_html: {value: true} %>
    </div>
  </div>
  <div class="row">
    <div class="form-actions col s8">
      <%= f.button :submit %>
    </div>
  </div>
  <% end %>
<% else %>

<%= simple_form_for(@user) do |f| %>
  <%= f.error_notification %>
  <div class="row">
    <div class="form-inputs">
      <%= f.input :role, as: :hidden, input_html: { value: "parent"} %>
      <%= f.input :username, label_html: { class: 'active' } %>
      <%= f.input :password, label_html: {class: 'active'} %>
      <%= f.input :password_confirmation, label_html: {class: 'active'} %>
      <%= f.input :phone, label_html: { class: 'active' } %>
      <%= f.input :email, label_html: { class: 'active' } %>
      <%= f.input :active, as: :hidden, input_html: {value: true} %>
    </div>
  </div>
  <div class="row">
    <div class="form-actions col s8">
      <%= f.button :submit %>
    </div>
  </div>
  <% end %>

<% end %>